apiVersion: v1
kind: ConfigMap
metadata:
  name: config-{{.Backstage.Name}}
  namespace: {{.Backstage.Namespace}}
data:
  SERVICE_URL: "https://{{.Backstage.Name}}.{{.Backstage.Namespace}}.svc"
  ROUTE_URL: "http://config-{{.Backstage.Name}}-{{.Backstage.Namespace}}.{{.OpenShift.IngressDomain}}"
